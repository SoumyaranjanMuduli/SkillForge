'use client'
import {useState} from 'react'
export function ProgramForm(){const [f,setF]=useState({id:'',slug:'',name:'',description:'',icon:'book',status:'active'});const [msg,setMsg]=useState('');async function save(e:any){e.preventDefault();setMsg('Saving…');const r=await fetch('/api/admin/programs',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});setMsg(r.ok?'Saved.':'Save failed.');if(r.ok)location.reload()}return <form onSubmit={save} className="card mb-5 p-5"><div className="grid gap-3 md:grid-cols-3"><input className="input" placeholder="ID" value={f.id} onChange={e=>setF({...f,id:e.target.value})} required/><input className="input" placeholder="Slug" value={f.slug} onChange={e=>setF({...f,slug:e.target.value})} required/><input className="input" placeholder="Name" value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/></div><textarea className="input mt-3 min-h-20" placeholder="Description" value={f.description} onChange={e=>setF({...f,description:e.target.value})}/><div className="mt-3 flex gap-3"><select className="input max-w-48" value={f.status} onChange={e=>setF({...f,status:e.target.value})}><option>active</option><option>archived</option></select><button className="btn-primary">Save program</button>{msg&&<span className="self-center text-sm text-slate-400">{msg}</span>}</div></form>}
