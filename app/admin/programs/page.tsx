import { AppShell, PageTitle } from '@/components/AppShell'
import { createAdminClient } from '@/lib/supabase/admin'
import { requireAdmin } from '@/lib/auth'
import { ProgramForm } from './program-form'
export default async function AdminPrograms(){await requireAdmin();const db=createAdminClient() as any;const {data}=await db.from('programs').select('id,slug,name,description,icon,status,created_at').order('name');const ids=(data??[]).map((x:any)=>x.id);const [{data:q},{data:a}]=await Promise.all([ids.length?db.from('questions').select('program_id').in('program_id',ids):{data:[]},ids.length?db.from('assessments').select('program_id').in('program_id',ids):{data:[]}]);return <AppShell role="admin"><PageTitle eyebrow="Admin / Programs" title="Program catalog" desc="Programs are live database records. Use the API to create, update, archive and activate them."/><ProgramForm/><div className="card overflow-auto"><table className="min-w-[900px] w-full text-sm"><thead className="bg-slate-50 text-left text-xs text-slate-500"><tr><th className="px-5 py-4">Program</th><th className="px-5 py-4">Slug</th><th className="px-5 py-4">Questions</th><th className="px-5 py-4">Assessments</th><th className="px-5 py-4">Status</th></tr></thead><tbody>{(data??[]).map((p:any)=><tr key={p.id} className="border-t border-line"><td className="px-5 py-4"><b>{p.name}</b><div className="text-xs text-slate-500">{p.description}</div></td><td className="px-5 py-4 text-slate-400">{p.slug}</td><td className="px-5 py-4">{(q??[]).filter((x:any)=>x.program_id===p.id).length}</td><td className="px-5 py-4">{(a??[]).filter((x:any)=>x.program_id===p.id).length}</td><td className="px-5 py-4">{p.status}</td></tr>)}</tbody></table></div></AppShell>}
