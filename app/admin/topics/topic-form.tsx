'use client'
import {useState} from 'react'
export function TopicForm({programs,topics}:{programs:any[];topics:any[]}){const [f,setF]=useState({programId:programs[0]?.id??'',parentId:'',name:'',slug:'',description:''});const [msg,setMsg]=useState('');async function save(e:any){e.preventDefault();setMsg('Saving…');const r=await fetch('/api/admin/topics',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...f,parentId:f.parentId||null})});setMsg(r.ok?'Saved.':'Save failed.');if(r.ok)location.reload()}return <form onSubmit={save} className="card mb-5 p-5"><div className="grid gap-3 md:grid-cols-4"><select className="input" value={f.programId} onChange={e=>setF({...f,programId:e.target.value})}>{programs.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select className="input" value={f.parentId} onChange={e=>setF({...f,parentId:e.target.value})}><option value="">No parent</option>{topics.filter(t=>t.program_id===f.programId).map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select><input className="input" placeholder="Topic name" value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/><input className="input" placeholder="slug" value={f.slug} onChange={e=>setF({...f,slug:e.target.value})} required/></div><div className="mt-3 flex gap-3"><button className="btn-primary">Create topic</button>{msg&&<span className="self-center text-sm text-slate-400">{msg}</span>}</div></form>}
